export enum EventTypes {
  stdout = "stdout",
  stderr = "stderr",
  bufferChange = "bufferChange",
  statusChange = "statusChange",
}

export enum ComputerStatus {
  busy = "busy",
  idle = "idle",
  off = "off",
  startup = "startup",
}
